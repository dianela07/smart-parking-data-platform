# ============================================
# CONFIGURACION DE ENTORNO
# ============================================
# Copia este archivo a .env y completa los valores
# NUNCA subas .env a git (ya esta en .gitignore)

# Base de datos PostgreSQL (produccion)
# DATABASE_URL=postgresql://usuario:contrasena@localhost:5432/parking_db

# Para desarrollo local, dejar vacio (usa SQLite automaticamente)
DATABASE_URL=

# API Keys (si se necesitan en el futuro)
# API_KEY=tu_api_key_aqui
